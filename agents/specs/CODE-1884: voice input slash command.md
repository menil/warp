# CODE-1884: Voice input V0 — TUI `/voice` slash command

*Spec: Voice input V0 — start recording from the headless TUI `/voice` command, show the footer-only mode indicator, and stop/transcribe with `Esc`*

== PRODUCT ==
*Summary:* Add a no-argument `/voice` slash command to the headless TUI composer in `crates/warp_tui`. Selecting the command or submitting an exact `/voice` immediately starts the existing microphone recorder. While the slash-originated session is active, the **only** voice indicator is the bottom-left footer/status row; the input gutter stays empty because the existing shell `!` gutter is suppressed and no voice glyph replaces it. `Esc` stops recording for transcription and the footer says `Transcribing...` until the result is inserted into the composer. This V0 does not add GUI behavior, CLI-agent rich-input behavior, or cloud-agent composition behavior.

*Key design choices:* Register `/voice` only when the shared command registry is built for `SettingsMode::Tui`; reuse the app's existing `voice_input::VoiceInput` and `VoiceTranscriber` singletons through a small TUI controller with an injectable test backend; make the footer status row the only voice indicator and suppress (but do not replace) the input's shell `!` gutter while voice is active; handle `Esc` in `TuiInputView` before shell-mode exit only for the TUI slash-originated session.

*Behavior* (numbered, testable invariants):
1. In a logged-in, local TUI session with AI enabled, voice team policy enabled, the `voice_input` capability compiled, and voice requests available, `/voice` appears in the TUI slash menu as a no-argument command. It is absent from GUI command results because the command is registered only for `SettingsMode::Tui`, absent from cloud-agent composition, and absent from the CLI-agent rich-input allowlist.
2. The command accepts no argument. Submitting `/voice` or `/voice` followed only by whitespace starts recording; `/voice text` does not start a session and reports the TUI's transient invalid-command hint. Selecting the menu row executes immediately, clears the command text, and never submits `/voice` as an AI prompt.
3. A successful selection/submission starts exactly one `VoiceInput::start_listening` session using the existing recorder and transcriber infrastructure. The TUI controller enters `Listening` only after the recorder starts successfully and emits a redraw event; it does not create a second recorder or a TUI-specific transcription service.
4. While slash-originated state is `Listening`, the input gutter is blank (the existing shell `!` is suppressed and no voice glyph/status is rendered), and the footer's first/only status text is `voice mode · esc to stop`. While state is `Transcribing`, the input gutter remains blank and the footer's status text is exactly `Transcribing...`. The footer status remains visible even when the composer buffer is non-empty.
5. Voice status has precedence over the existing shell affordances: a voice-active footer never renders `SHELL_MODE_HINT` (`shell mode · esc to exit`), and a voice-active input never renders the `!` shell gutter or any replacement glyph. With voice stopped, shell mode continues to render both existing affordances. Other existing footer priorities (for example, ctrl-c exit confirmation) remain unchanged unless the implementation documents and tests a direct conflict.
6. Pressing `Esc` while slash-originated `Listening` calls the successful `VoiceInput::stop_listening` path, not `abort_listening`, and transitions the TUI controller to `Transcribing` while WAV conversion/transcription is pending. The contextual input Escape binding consumes the key so it does not also exit shell mode or dismiss the voice state.
7. Pressing `Esc` while `Transcribing` is a handled no-op: the in-flight transcription future is not aborted and the footer remains `Transcribing...` until completion. An inline menu, if one is open due to a stale redraw, still gets the existing first-Escape dismissal priority.
8. On a successful transcription, the returned text is inserted into the existing TUI composer through `CodeEditorModel`/`user_insert`, the voice state returns to idle, the voice footer disappears, the normal shell gutter/footer behavior resumes, and normal agent-input rendering resumes without submitting the text automatically. The `/voice` command text is not retained in the buffer.
9. On microphone denial, recorder/WAV failure, missing transcriber, quota exhaustion, or server transcription failure, the controller returns to idle, clears the voice affordance, and uses a concise `TransientHint` in the TUI footer; it never leaves the input locked or leaves a stale transcription future able to mutate a later session.
10. Existing TUI slash-command selection, exact-match parsing, shell-mode entry/exit, inline-menu Escape priority, footer transient hints, and all non-voice GUI voice behavior remain unchanged. A voice session is not available while a CLI agent owns input or while the active routing is cloud-agent composition.

== TECH ==
*Context:* The client source is pinned at `warpdotdev/warp` commit `5a25993a77ef87089f266942d5198610eaa369b4`.

- `crates/warp_tui/src/slash_commands.rs:1-382 @ 5a25993a77ef87089f266942d5198610eaa369b4` owns the TUI slash-menu model, exact-match lifecycle, row snapshots, and `AcceptSlashCommandOrSavedPrompt` payloads.
- `crates/warp_tui/src/terminal_session_view.rs:2190-2490 @ 5a25993a77ef87089f266942d5198610eaa369b4` parses submitted input, resolves selected registry IDs, applies `SlashCommandSelectionBehavior`, and exhaustively dispatches `TuiSlashCommand`; `:2150-2300` also contains `SHELL_MODE_HINT`, `render_status_footer_row`, and the replacing footer-hint priority.
- `crates/warp_tui/src/terminal_session_view.rs:2860-3120 @ 5a25993a77ef87089f266942d5198610eaa369b4` composes the inline menu, input view, and one-row footer. This is the owner that should hold/subscribe to the TUI voice controller and pass its state to the footer.
- `crates/warp_tui/src/input/view.rs:1-180,198-396,631-790 @ 5a25993a77ef87089f266942d5198610eaa369b4` owns the char-cell editor, shell `!` gutter, input keymap context, inline-menu priority, `Esc` handling, and render branch. Voice handling must add only contextual `Esc`/keymap behavior and suppression of the existing shell gutter while active; it must not add a voice-glyph render branch, while preserving the single `MouseStateHandle` ownership rule.
- `crates/warp_tui/src/slash_commands_tests.rs:1-358 @ 5a25993a77ef87089f266942d5198610eaa369b4`, `crates/warp_tui/src/input/view_tests.rs:1-950 @ 5a25993a77ef87089f266942d5198610eaa369b4`, and `crates/warp_tui/src/terminal_session_view_tests.rs:1-1258 @ 5a25993a77ef87089f266942d5198610eaa369b4` already provide render-to-lines, slash selection, footer, shell-gutter, keymap-context, and Escape test fixtures.
- `app/src/search/slash_command_menu/static_commands/commands.rs:1-199,560-769 @ 5a25993a77ef87089f266942d5198610eaa369b4` defines `StaticCommand`, the registry, `all_commands(SettingsMode)`, and the existing TUI-only `/mcp`, `/exit`, and `/view-logs` registrations. Add `VOICE` to the TUI branch only, with `argument: None`, `Availability::AI_ENABLED | Availability::NOT_CLOUD_AGENT`, and a voice icon/description.
- `app/src/terminal/input/slash_commands/mod.rs:122-175 @ 5a25993a77ef87089f266942d5198610eaa369b4` is the shared TUI allowlist (`TuiSlashCommand`, `from_static_command`, `slash_command_is_supported_in_tui`); add `Voice` and keep its match exhaustive.
- `app/src/terminal/input/slash_commands/data_source/tui.rs:1-145 @ 5a25993a77ef87089f266942d5198610eaa369b4` filters registry commands for TUI and `data_source/core.rs:45-155,302-440` owns shared availability subscriptions. Add the voice-specific `AISettingsChangedEvent::VoiceInputEnabled`, `UserWorkspacesEvent::TeamsChanged`, and `AIRequestUsageModelEvent::RequestUsageUpdated` recomputation triggers, plus a race-safe start-time gate using `AISettings::is_voice_input_enabled`, `UserWorkspaces::is_voice_enabled`, and `AIRequestUsageModel::can_request_voice`.
- `app/src/tui_export.rs:1-190 @ 5a25993a77ef87089f266942d5198610eaa369b4` is the public boundary used by `warp_tui`. Re-export the feature-gated `VoiceInput`, `VoiceInputState`, `VoiceSession`, `VoiceSessionResult`, `VoiceInputToggledFrom`, `StartListeningError`, `VoiceTranscriber`, `Transcriber`, and `TranscribeError` types needed by the TUI adapter; do not expose GUI editor views.
- `app/src/lib.rs:1995-2008,1608-1630 @ 5a25993a77ef87089f266942d5198610eaa369b4` already registers `VoiceInput` when the app `voice_input` feature is enabled, always registers `VoiceTranscriber`, and registers `AIRequestUsageModel`. Enable `voice_input` in the app's `tui` feature and in the `warp_tui` dependency feature list (`crates/warp_tui/Cargo.toml:69-80`) so the TUI uses those singletons rather than a second backend.
- `crates/voice_input/src/lib.rs:35-88,130-154,280-371 @ 5a25993a77ef87089f266942d5198610eaa369b4` defines `VoiceInputState`, `VoiceSession`, `start_listening`, `stop_listening`, and `abort_listening`. `app/src/voice/transcriber.rs:1-48` and `app/src/server/voice_transcriber.rs:1-41` define the existing async transcription boundary.
*Headless-TUI feasibility findings (verified against the source at the pinned commit):*
- The recorder is not coupled to a GUI `ViewContext`, `WindowHandle`, GPU scene, or GUI editor. `VoiceInput::start_listening` takes only `&mut ModelContext<Self>` plus `VoiceInputToggledFrom`; it calls native `cpal::default_host`, obtains the default input device/config, builds/plays a stream, feeds samples through `ModelContext::spawn_stream_local`, resamples with `rubato::SincFixedIn`, and stores a `VoiceSession` backed by a `futures::channel::oneshot`. `stop_listening` pauses cpal, converts the resampled samples to 16 kHz/16-bit mono WAV/base64 in a spawned future, and sends `VoiceSessionResult`; these operations are model/background-executor APIs that the headless event loop can drive.
- `VoiceInput` does consult `ctx.microphone_access_state()` before creating the active stream. The platform API is intentionally frontend-neutral (`warpui_core::platform::Delegate`), but the current headless delegate at `crates/warpui/src/platform/headless/delegate.rs:180-187 @ 5a25993a77ef87089f266942d5198610eaa369b4` returns `MicrophoneAccessState::Denied` unconditionally. Because `start_listening` maps `Denied`/`Restricted` to `StartListeningError::AccessDenied`, the current TUI cannot record even if cpal and the `voice_input` Cargo feature are linked. This is a confirmed blocker, not an assumption.
- The implementation must add a **TUI-only** headless permission/device shim before claiming production TUI recording works; it must not alter GUI microphone-permission behavior or shared GUI voice code paths. The headless delegate must report a real platform state (or use a narrowly scoped recorder-owned permission probe) and must not require a GUI window or GUI event loop. The shim must preserve macOS authorization semantics (the GUI delegate reads `AVCaptureDevice::authorizationStatusForMediaType`), define Linux/FreeBSD/Windows behavior where cpal itself is the availability probe, and return a deterministic denied/restricted state when the OS/backend cannot provide capture. A user-facing `NotDetermined` state must be handled explicitly rather than treated as authorized.
- `ModelContext::spawn_stream_local` and `ModelContext::spawn` deliver callbacks through the app's headless event loop, which is present in `warpui/src/platform/headless/event_loop.rs`; no GPU rendering or `WindowHandle` is required. The live path therefore needs a running `run_tui` event loop, but not a GUI display/event loop. Tests must inject a fake backend/session so they do not require cpal hardware.
- `VoiceTranscriber` is a singleton wrapper around `Arc<dyn Transcriber>`. `ServerVoiceTranscriber` implements that trait by building the existing `TranscribeRequest` and calling `ServerApi::transcribe` (the `/ai/transcribe` boundary); this code has no GUI/editor dependency. `initialize_app` registers `VoiceInput` only under `#[cfg(feature = "voice_input")]`, always registers `VoiceTranscriber`, and registers `AIRequestUsageModel`; `run_tui` invokes that same initialization before mounting the TUI.
- The current Cargo graph does not link the recorder into the TUI: `app`'s `tui` feature is only `warpui_core/tui`, `gui` is the feature that currently enables `voice_input`, and `crates/warp_tui/Cargo.toml` depends on `warp` with only `image_as_context,tui`. The implementation must explicitly enable `voice_input` in both the app TUI feature and the `warp_tui` `warp` dependency (or prove an equivalent unified feature path), then verify with Cargo metadata/build output that `cpal`, `rubato`, `hound`, and the app singleton registrations are present in the TUI binary.
- The production feasibility gate is therefore: feature wiring/build succeeds; the headless permission shim reports the correct state; cpal can open a real default input device on the target; the existing resampling/WAV/session pipeline completes; and `ServerVoiceTranscriber` can reach `/ai/transcribe`. Until all four are demonstrated, `/voice` must fail cleanly with the TUI transient hint and must not pretend that recording started.

*Design alternatives*:
- *Register `/voice` in the shared command list for every frontend.* Rejected: this is explicitly TUI-only and `SettingsMode::Tui` already provides a registry branch for TUI-only commands. The chosen placement prevents GUI menu exposure while still allowing the normal TUI data-source path to discover it.
- *Implement a second recorder/transcription client in `warp_tui`.* Rejected: it would duplicate microphone permissions, WAV conversion, authentication, quota handling, and server API behavior. The chosen controller adapts the already-registered app singletons.
- *Put all state in `TuiTerminalSessionView` and make the input view emit a generic Escape event.* Rejected: Escape precedence and render state would be split across two owners. The chosen `TuiVoiceInputModel` is shared by the session owner and `TuiInputView`, emits explicit state events, and lets the input consume contextual Escape before shell-mode exit.
- *Use the input `!` gutter as a voice indicator.* Rejected: the requester explicitly requires no voice indicator in the gutter. The footer row is the ticket's bottom-left shell “hint” (`SHELL_MODE_HINT`) and is the only voice indicator; the existing shell `!` is suppressed during voice so the gutter is blank, with separate render-to-lines assertions.
- *Use the real microphone/network in unit tests.* Rejected: it is nondeterministic and unsuitable for CI. The chosen controller depends on a narrow `TuiVoiceBackend` adapter; production wraps the existing app singletons, while tests inject fake sessions/results and a fake transcriber.

*Proposed changes:*
1. Add `VOICE` to `commands.rs` and push it only when `settings_mode == SettingsMode::Tui`; add `TuiSlashCommand::Voice` and its registry conversion. Preserve the existing CLI-agent allowlist and cloud-mode availability gates.
2. Extend `TuiSlashCommandDataSource` with voice-specific availability filtering and subscriptions. The menu must hide the command when compile-time voice support, AI settings, team policy, or cached voice quota disallows it; `execute_tui_slash_command` must repeat the gates immediately before starting to cover stale menu results.
3. Enable the app's optional `voice_input` dependency for the TUI build and add the minimal `tui_export` re-exports. No GUI editor/input files are in scope; existing GUI registrations and semantics remain the compatibility contract.
4. Add a TUI-local `TuiVoiceInputModel` (for example `crates/warp_tui/src/voice_input.rs`) with `Idle`, `Listening`, and `Transcribing` states, an explicit `SlashCommand` origin, a generation/session identity, and events for state changes, transient errors, and completed text. Its production backend starts `VoiceInput::start_listening` (using the existing singleton), awaits `VoiceSession`, calls `stop_listening` on `Esc`, and delegates audio to the existing `VoiceTranscriber`; it must clear/ignore stale futures when a session ends.
5. Construct the controller beside the TUI input editor in `TuiTerminalSessionView::new`, pass its handle into `TuiInputView`, subscribe the parent to redraw/state events, and add the `TuiSlashCommand::Voice` branch that clears `/voice`, starts the controller, and records normal TUI slash-command telemetry.
6. Add a voice-active flag in `input_keymap_context` and a voice-aware branch in `handle_escape` after inline-menu dismissal and before `is_shell_mode`. Listening calls the controller's successful stop; Transcribing returns handled without cancellation; idle falls through to current shell Escape behavior. While active, suppress the existing `!` shell gutter and render no replacement glyph; keep the existing shell gutter unchanged when voice is idle.
7. Extend `TuiTerminalSessionView::render_footer`/`render_status_footer_row` with the canonical and only voice status before normal shell sections. `Listening` renders `voice mode · esc to stop`; `Transcribing` renders `Transcribing...`; neither renders the shell hint, model/usage sections, or a stale shell prefix. Use `TuiUiBuilder` semantic styles and one-row truncation.
8. On successful text, insert via the existing char-cell editor model and restore normal agent input. On all error/aborted paths, clear the state and show a TUI `TransientHint`; do not reuse GUI-only toast/view types. Preserve the shared GUI voice code's existing behavior.

*Open questions resolved:* “TUI only” means the headless `crates/warp_tui` frontend and its app-facing exports/feature wiring, not `app/src/editor` or GUI terminal input. The requested bottom-left “shell command hint” is the footer constant `SHELL_MODE_HINT`; because the same shell mode also renders a `!` gutter, voice suppresses both to avoid contradictory simultaneous affordances. `/voice` starts immediately on menu acceptance or exact submission and leaves the transcribed text in the composer rather than submitting it. Voice availability follows existing AI/team/settings/quota gates, while microphone permission and server failures are runtime transient hints. No new slash command is offered in CLI-agent rich input or cloud composition.

*Affected files:* `app/src/search/slash_command_menu/static_commands/commands.rs`; `app/src/terminal/input/slash_commands/mod.rs`; `app/src/terminal/input/slash_commands/data_source/tui.rs` and relevant shared data-source tests; `app/src/tui_export.rs`; `app/Cargo.toml`; `crates/warp_tui/Cargo.toml`; `crates/warp_tui/src/voice_input.rs` (new); `crates/warp_tui/src/input/view.rs`; `crates/warp_tui/src/terminal_session_view.rs`; `crates/warp_tui/src/slash_commands_tests.rs`; `crates/warp_tui/src/input/view_tests.rs`; `crates/warp_tui/src/terminal_session_view_tests.rs`; `app/src/terminal/input/slash_commands/mod_tests.rs`; `crates/warpui/src/platform/headless/delegate.rs` or an equivalent platform-neutral microphone-access seam; and focused fake-backend/controller/feasibility tests. `crates/voice_input` changes are not expected unless the adapter requires a narrowly-scoped public test seam.

*Risks / blast radius:* Enabling `voice_input` for a headless build adds native microphone/CPAL dependencies and may expose platform build failures; validate all supported TUI targets and keep the dependency behind the TUI feature. The current headless delegate always reports microphone denial, so no production claim is valid until the required permission/device shim is implemented and exercised. Async WAV/transcription completion can race a later session; use a generation token, clear it on every terminal/error path, and ignore stale callbacks. The footer and input render trees can regress shell precedence; centralize `voice_active` checks and assert exact lines. Team/settings/quota changes can race a populated slash menu; gate both active-command recomputation and execution. A missing transcriber, microphone denial, quota error, or server failure must always restore idle input.

*Validation & verification criteria* (must ALL pass before merge):
1. A static-command test proves `commands::VOICE` has exactly the `/voice` name, no argument, AI/non-cloud availability, and is included only when the registry uses `SettingsMode::Tui`; a GUI-mode registry test proves it is absent.
2. `app/src/terminal/input/slash_commands/mod_tests.rs` proves `/voice` maps to `TuiSlashCommand::Voice`, is supported in TUI, executes immediately under `slash_command_selection_behavior`, and remains absent from the CLI-agent allowlist and cloud-mode results.
3. `crates/warp_tui/src/slash_commands_tests.rs` proves the real TUI data source/menu row renders `/voice`, exact `/voice` and whitespace parsing execute, `/voice text` is rejected, and accepting the row emits an execute action rather than inserting command text.
4. A fake-backend controller test proves a valid `/voice` dispatch starts exactly once, enters `Listening`, clears the command buffer, emits a redraw, and never submits an AI prompt; a second dispatch while active does not start a second recorder.
5. A fake-backend Escape test starts slash-originated `Listening`, dispatches `TuiInputAction::HandleEscape`, and asserts `stop_listening`/successful stop was called, `abort_listening` was not, state is `Transcribing`, and the input keymap consumes Escape. A second Escape test proves an in-flight transcription remains active and uncancelled.
6. Render-to-lines tests cover idle, Listening, and Transcribing input/footer states. Listening must show `voice mode · esc to stop` only in the footer, with a blank input gutter and no voice glyph; Transcribing must show exact `Transcribing...` only in the footer, with the same blank gutter. Neither active state may contain `!` or `shell mode · esc to exit`. The idle control case must retain the current shell gutter/footer text.
7. A completion test resolves a fake successful transcription, asserts the text is inserted through `CodeEditorModel`, the `/voice` text is absent, the footer-only voice status is cleared, and a subsequent normal submission still works. Failure tests cover microphone denial, WAV conversion/aborted session, missing transcriber, quota, server overload/transport, and stale completion after a new session; each returns to idle and shows a deterministic transient hint.
8. Availability tests toggle `AISettingsChangedEvent::VoiceInputEnabled`, `UserWorkspacesEvent::TeamsChanged`, `AIRequestUsageModelEvent::RequestUsageUpdated`, CLI-agent input, and cloud routing; `/voice` must recompute to the correct visible/hidden state and dispatch must re-check the same gates.
9. Existing TUI tests continue to pass, including slash-menu lifecycle, shell `!` rendering, footer section/transient priority, `input_escape_context_is_present_only_while_escape_is_handled`, `escape_dismisses_menu_and_closed_menu_submit_falls_through`, and all terminal-session tests. Existing GUI voice tests and hold-to-talk/button Escape behavior must also remain unchanged.
10. Compile the TUI with voice support and prove the feature is actually linked: `cargo tree -p warp_tui --features standalone -e features` (or equivalent metadata query) must show the `warp/voice_input` feature and `cpal`, `rubato`, and `hound`; `CARGO_BUILD_JOBS=2 cargo build -p warp_tui --bin warp-tui-oss --features standalone`, `cargo nextest run -p warp_tui`, and the affected `warp` tests with `--features tui,voice_input` (or the repository's equivalent package/test filters) must pass. No physical microphone or network service may be required for unit tests.
11. Run `./script/format`, the repository's required `cargo clippy --workspace --all-targets --all-features --tests -- -D warnings`, and `./script/presubmit`; all must pass without new warnings.
12. Perform live TUI verification after a successful build in a real PTY (for example `./script/run-tui` under tmux, using `tmux send-keys` and `tmux capture-pane -p`): first run an authenticated local fixture with the production backend and a permitted default microphone to prove the headless permission shim reports the correct state, cpal opens the device, audio reaches resampling/WAV conversion, and `ServerVoiceTranscriber` reaches `/ai/transcribe`; then observe `/voice` in the slash menu, `voice mode · esc to stop` while recording, a blank input gutter with no voice glyph and no `!`, `Transcribing...` after `Esc`, insertion of returned text without auto-submit, and return to the normal footer. If the host has no usable microphone, record the environment-mismatch skip and run the same PTY flow against the fake backend; the implementation must still show a deterministic transient hint for real `AccessDenied`/device failures. Rendered terminal output/captured pane is the source of truth; no GUI/computer-use screenshot is required.
13. The draft PR remains on `warpdotdev/warp` and the existing `factory/voice-input-v0` branch, contains this single spec file until implementation, retains the originating Slack thread (`https://warpdotdev.slack.com/archives/C0BDQDW8V5E/p1784659365147889`), has exactly one valid factory metadata block, remains draft, and assigns `harryalbert` as reviewer.
